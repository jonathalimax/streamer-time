class AgendaController {
  get(req, res) {
    return res
      .status(200)
      .json({});
  }
  getAll(req, res) {
    return res
      .status(200)
      .json({});
  }

  getByStreamerId(req, res) {
    return res
      .status(200)
      .json({});
  }
}

export default new AgendaController();
